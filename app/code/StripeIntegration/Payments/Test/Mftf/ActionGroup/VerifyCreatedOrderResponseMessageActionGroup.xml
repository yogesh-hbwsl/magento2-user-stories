<?xml version="1.0" encoding="UTF-8"?>
<actionGroups xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="urn:magento:mftf:Test/etc/actionGroupSchema.xsd">
    <actionGroup name="VerifyCreatedOrderResponseMessageActionGroup">
        <annotations>
            <description>Validates that the Error Message is present and correct.</description>
        </annotations>

        <see selector="{{AdminStripeCheckoutMessageSection.errorMessage}}" userInput="This payment method cannot be used because it requires a customer authentication. To avoid authentication in the admin area, please contact Stripe support to request access to the MOTO gate for your Stripe account." stepKey="seeErrorMessage"/>
    </actionGroup>
</actionGroups>
